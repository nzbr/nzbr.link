{
  "private": true,
  "scripts": {
    "prepublish": "run-script-os",
    "prepublish:windows": "wsl -d NixOS sh -c \"nix run nixpkgs#yarn2nix > yarn.nix\"",
    "prepublish:default": "if hash nix; then nix run nixpkgs#yarn2nix > yarn.nix; fi"
  },
  "dependencies": {
    "@fontsource/roboto-slab": "^4.5.8",
    "@mdi/svg": "^6.7.96"
  },
  "devDependencies": {
    "prettier": "^2.7.1",
    "run-script-os": "^1.1.6",
    "sass": "^1.52.3"
  }
}
